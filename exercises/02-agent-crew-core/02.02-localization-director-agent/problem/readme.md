# Problem 02.02: In-Character Translation with Rationale

Implement `LocalizationDirectorAgent` ensuring idioms are adapted for target locales (e.g. Hindi, Spanish) and every line includes a non-empty `rationale` field.
