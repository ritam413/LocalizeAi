# Problem 03.02: Targeted Retry Routing

Implement `DirectorAgent.run_targeted_retry(defect_finding)` sending only the defective scene back to the target agent, asserting that retry count increments and status updates to 'fixed'.
